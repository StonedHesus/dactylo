// The package in which the current Java compilation unit is to be found.
package upc.coop.dactylo.settings;// Imports from existing Java libraries, classes and interfaces.

// Import from custom libraries, classes and interfaces.

/**
 * @author Andrei-Paul Ionescu
 */
public interface Settings {
    // Settings concerning the window.
    int DEFAULT_WINDOW_WIDTH = 1280;
    int DEFAULT_WINDOW_HEIGHT = 842;
    String APPLICATION_TITLE = "Dactylo";
}
